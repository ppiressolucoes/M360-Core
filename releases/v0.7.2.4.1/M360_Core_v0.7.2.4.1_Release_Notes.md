# M360 Core v0.7.2.4.1 — Renderer Canary UX Hotfix

## Correção

- corrige o rótulo administrativo que ainda mostrava v0.7.2.3;
- inclui um aviso no topo do painel com o estado da lista de posts autorizados;
- mantém intactos o contrato, os shortcodes, as regras de segurança e o storage da v0.7.2.4.

## Comportamento esperado

Com a lista vazia, o shortcode do canário retorna vazio por segurança. Salve `7804, 7807` na seção **Renderer Canary** do mesmo painel e teste o shortcode no template de post único.
